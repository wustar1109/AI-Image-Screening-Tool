# 筛图魔方 - AI 智能图片筛选工具

## 项目概述

**筛图魔方** 是一款基于 AI 视觉大模型的浏览器端图片筛选工具。用户上传照片后，AI 自动分析每张图片的质量、构图、风格匹配度等维度，输出评分和筛选建议（A~E 等级），帮助用户快速从大量照片中挑出优质作品。

所有分析在浏览器内完成，图片存储在本地 IndexedDB，支持离线使用。

---

## 技术架构

| 层级 | 技术选型 |
|------|---------|
| 前端框架 | React 18 + TypeScript |
| 构建工具 | Vite |
| 样式方案 | Tailwind CSS（自定义 dark / accent 色板） |
| 状态管理 | Zustand（localStorage 持久化） |
| 图片存储 | IndexedDB（base64 Data URL） |
| AI 模型 | Qwen/Qwen2.5-VL-7B-Instruct（OpenAI 兼容 API） |
| 本地算法 | Canvas 基础 CV 特征提取 |

---

## 核心功能模块

### 1. 我的主页（Dashboard）
- 统计概览：累计处理照片数、淘汰废片数、节省选片时间、精选照片数
- 快捷入口：一键开始筛选、批量修图
- 项目网格：所有历史项目卡片（2×2 缩略图预览），支持收藏、重命名、删除
- 搜索：按项目名称或文件名搜索

### 2. 图片筛选（Screening）
- **多种上传方式**：拖拽、点击选择、文件夹上传、ZIP 压缩包自动解压
- **并发分析**：线程池控制（默认 2 并行），加速批量处理
- **图片压缩**：上传前自动缩放至最大 896px JPEG，减少 API 传输量
- **QwenVL 任务开关**：用户可自由开启/关闭以下任务
  - 模板分类（自动识别风格模板）
  - 模板审稿（结构失控/伪影/焦点检测）
  - 多维评分（结构/构图/节奏/匹配/工艺/伪影 6 维度）
  - 最终标签（A~E 等级映射）
  - 总结原因（一句话总结 + 2~4 条原因）
- **进度追踪**：实时显示处理百分比和最新分析结果
- **评分融合**：CV 本地评分 60% + Qwen 评分 40%（双启用时）

### 3. 筛选结果（Results）
- 结果卡片：每张图片展示缩略图、文件名、AI 评语、结构化评分
- 筛选过滤：全部 / 精选 / 淘汰
- 统计标签：精选数量、淘汰数量
- 导出报告：一键下载 TXT 格式筛选报告
- 项目管理：重命名、收藏、删除

### 4. 分类图库（Gallery）
- **自动分类**：按 AI 识别的风格模板自动分组（极简抽象、丝绸流体、几何、自然花卉、山川湖海等）
- **模板管理器**：
  - 5 个预定义模板（不可删除）
  - 自定义模板增删改
  - AI 识别到新风格时自动创建模板
- **图片详情模态框**：查看大图、下载原图、查看分类置信度、总分、标签
- **批量选择 + ZIP 导出**：跨模板多选，打包导出并附带分类报告
- **搜索**：按图片名、模板名、项目名称搜索

### 5. 我的收藏（Favorites）
- 收藏项目列表，支持取消收藏和删除
- 跳转查看完整筛选结果

### 6. 批量修图（BatchEdit）
- 批量图片上传与预览
- （功能框架已搭建，具体修图能力待扩展）

### 7. 设置（Settings）
- **多 API 服务管理**：支持配置多个 API 服务商（SiliconFlow 等），切换使用
- **模型自动探测**：输入 API Key 后自动拉取可用模型列表
- **连接状态检测**：30 秒轮询 API 在线状态
- **Qwen 任务配置**：开关各分析维度，控制 token 消耗

---

## 数据流与存储

```
用户上传图片
    ↓
IndexedDB（持久化存储原始图片）
    ↓
本地 CV 特征提取（结构/纹理/清晰度）
    ↓
图片压缩（max 896px JPEG）
    ↓
QwenVL API 调用（并发池 2 个）
    ↓
结果融合（CV 60% + Qwen 40%）
    ↓
结构化结果保存到 Zustand historyStore（localStorage）
    ↓
分类图库自动按模板分组展示
```

### 存储说明
| 数据 | 存储位置 | 持久化 |
|------|---------|--------|
| 历史记录 / 筛选结果 | Zustand → localStorage | ✅ 刷新不丢失 |
| 模板配置 | Zustand → localStorage | ✅ 刷新不丢失 |
| 上传的图片 | IndexedDB | ✅ 刷新不丢失 |
| API 设置 | Zustand → localStorage | ✅ 刷新不丢失 |

---

## 关键设计决策

1. **浏览器端分析**：所有 AI 调用通过前端直接请求 API，无需后端服务器，`server/` 目录未使用
2. **单表 IndexedDB**：所有图片存入同一 objectStore，通过 `recordId_前缀` 分组，支持单次 `getAll()` 批量读取
3. **动态模板注入**：Qwen 的模板分类 prompt 不是写死的 5 个模板，而是实时读取 templateStore 中的全部模板（包括用户自定义和自动创建的）
4. **软硬结合**：保留本地 CV 评分作为快速筛选的第一道关卡，Qwen 作为语义理解的深度补充
5. **移除硬拒绝**：所有图片都进入完整评分流程，最终 keep/discarded 由分数阈值决定

---

## 项目目录结构

```
ai筛图工具/
├── client/                    # React 前端（Vite）
│   ├── src/
│   │   ├── pages/             # 7 个页面组件
│   │   ├── components/        # Sidebar、Header、Layout
│   │   ├── stores/            # Zustand 状态管理（6 个 store）
│   │   ├── services/          # AI 分析服务 + API 探测
│   │   ├── utils/             # IndexedDB + 图片压缩 + 并发池
│   │   ├── types/             # TypeScript 类型定义
│   │   ├── App.tsx            # React Router 路由配置
│   │   ├── index.css          # Tailwind + 全局基础样式
│   │   └── main.tsx           # 应用入口
│   ├── index.html
│   ├── vite.config.ts
│   ├── tailwind.config.js     # 自定义色板
│   └── package.json
├── server/                    # 未使用（预留后端目录）
├── package.json               # 根目录 scripts（concurrently）
└── .gitignore
```

---

## 构建与运行

```bash
# 安装依赖
npm install

# 启动开发服务器（client + 根目录并行）
npm run dev

# 生产构建
npm run build

# 类型检查
cd client && npx tsc --noEmit
```

**构建产物**：`client/dist/`（~380KB JS + ~26KB CSS）

---

## 当前状态

- ✅ 7 个页面全部可用
- ✅ AI 筛选流程端到端跑通
- ✅ 分类图库 + 模板管理完整闭环
- ✅ TypeScript 编译通过，Vite 生产构建成功
- ✅ 代码已提交到本地 Git（`bd22e8a`）
- ⏳ 待推送到 GitHub（需用户登录授权）

---

## 后续可扩展方向

1. **批量修图**：接入图片裁剪、尺寸调整、格式转换、水印添加
2. **结果对比**：左右对比原图与 AI 评语，快速决策
3. **导出格式**：支持 PDF 报告、Excel 评分表
4. **多模型支持**：除了 Qwen，可接入 Claude、GPT-4V 等
5. **离线模式**：完全断开 API 时，仅用本地 CV 评分兜底
